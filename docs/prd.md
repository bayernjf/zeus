# Zeus 产品需求文档（PRD）

> 状态：**现行 v0.40（2026-09-26，deferred #23 销项：DAG 驾驶员入口接 H2——`POST /api/intents` 接受可选 `dag:{nodes}` + `GET /api/intents/:id/dag` 回分层与关键路径，全量 711 绿 / 74 文件，typecheck/build exit 0）**；**上一条 v0.39（2026-09-26，deferred #15 销项：支持矩阵与 `engines` 落地——`engines.node: ">=22.0.0"` + `.npmrc` 的 `engine-strict=true` + `.nvmrc` 22 + CI 矩阵换 `[22.x, 24.x]`；纯配置改动，代码与测试零改动）**；上一条 v0.38（2026-09-26，deferred #16 销项：CI 构建机由浮动标签 `ubuntu-latest` 钉为 `ubuntu-24.04`，避开 2026-10-19 的无人批准换机；纯工作流一行 + 注释，代码与 Node 矩阵未动）**；上一条 v0.37（2026-09-26，离线名册验签的命令行入口 + 并发断言去墙钟化 + 那条未复现的测试失败已定位；deferred #23/#24 登记，全量 704 绿 / 73 文件）**；上一条 v0.36（2026-09-26，修 E4.8 状态列与正文的自相矛盾：列写 🚧 而正文与 v0.33 均记「已 PASS 关闭」；改后 P0 26/26 全绿，全库无 P0 未闭合项）**；上一条 v0.35（2026-09-25，签名链 v1.2：载荷 `schemaVersion` + 验签版本闸（`seal.v`/`attestation.v` 自 v1 就写入却从不校验，现已强制），deferred #22 销项，全量 696 绿 / 72 文件；上一条 v0.34（2026-09-25，评审 v0.12 抓到的执行 Agent 注册错误分类缺陷已修：transport 失败 → 502 且带 URL，按错误身份分类取代消息正则；全量 692 绿 / 72 文件）；上一条 v0.33（E4.8 首次真机验收 PASS + docker 实构实跑 PASS；评审 v0.11：MVP 维持 ✅，剩余收窄为 loom 联调/Jev key/RSK 托管/#18）；上一条 v0.32（三纲执行点收口：执行 Agent 出站凭证注入、Skill 注册中心上派发路径、MCP 服务端 tools + 客户端调用面；评审 v0.10 判 MVP = ✅ 库内达成）；上一条 v0.31（评审 v0.9 之后的首跑正确性批次：审计失败不再冒充派发失败、两扇门的 realm 语法统一、配置面可读）；上一条 v0.30（备份清单盖到内核状态文件 + 企业写凭证签名且一次性，deferred #13/#14 销项）；上一条 v0.29（E9.1/E9.2 上岗四道门与首日简报）；上一条 v0.28（企业域三级租户隔离 + 双域授权与审计呈现，E3.6/E6.4 收口）；v0.25（E4.8 阻塞归因纠偏，状态仍 🚧）；上一条 v0.24（2026-09-25，E1.5 有界并发队列 + E4.7 审计落盘可读 + E3.5 企业域收口 + 连接器/决策面接线 + 快照 0600）
> 上游：[product-portrait.md](product-portrait.md)（愿景与设计哲学的单一事实源，本文件不复制愿景全文）。
> 边界：本文件回答「做什么、优先级、验收标准」；「怎么建」看 `docs/design-*.md`；「做到哪」看 [handoff.md](../handoff.md)。
> 状态图例：✅ 已落地（有测试）｜🚧 部分落地 / 有脚手架｜⬜ 未启动。

## 1. 背景与问题

- **核心命题**：现实业务与生活任务越来越需要多个专业 Agent **同时开工、共同参与一个决策**，而现有产品要么是单 Agent 串行问答，要么并发能力薄弱、决策不可追溯。市场缺一个**高并发多 Agent 协同决策平台**作为内核。
- 个人用户：工作提效之外，记忆与数据散落各 SaaS，**无主权、不可备份、不可传承**；情绪层面缺少"被记住、被保护"的长期载体。
- 企业用户：Agent 能力点状存在，新员工上岗成本高，知识随人流失；缺少员工可指挥、可监督、且对业务结果负责的多 Agent 协作层。
- 供给侧：已有产品矩阵（agent-world、job-agent、agent-dev、pr-helper、atlas、loom；展示站 bayjf）各自独立，用户被迫面对一整组工具，产品之间存在隔阂。

## 2. 产品目标与非目标

### 2.1 目标

1. **（核心）高并发多 Agent 协同决策**：一个意图可扇出多个 Agent 并行执行，结果可聚合、冲突可消解、决策可追溯，操作者只面对一个合并视图。
2. Skill 作为显式的一等模块：能力以 Skill 声明、安装、加固、传授，协同决策按技能自动组队。
3. 用户给一个目录即可用：目录即数据库，先连接、经接口访问，备份为第一公民。
4. 用户只面对 Zeus 一扇门：矩阵产品以执行 Agent 身份归属声明，星型编排、可吊销、名册可验。
5. 双态闭环：个人备份清单真实可恢复；企业员工以操作者身份上岗、Mentor 带业务、全程审计。

### 2.2 非目标（Non-goals）

- 不自造 MCP/A2A 之外的私有封闭协议；Zeus v1 不做 A2A server（发起方永远是 Zeus/操作者）。
- Realm 不做独立 HTTP API（对外唯一传输为 MCP）。
- 不做执行 Agent 之间的 P2P 直连；不做企业→个人数据域的任何互通（无例外）。
- 传承的法律框架与企业计费模型不在 v1 实施（deferred #3/#4）。

## 3. 用户与核心场景

| 用户 | 核心场景 | 承诺 |
|---|---|---|
| 个人用户 | 一个意图 → 多个 Agent 并行处理工作/生活数据；日记/备份清单备份与恢复；可选传承 | 被记住、被保护、不被绑架 |
| 员工（操作者） | 监督多 Agent 协同决策，在冲突/升级点裁决；就任即由已认证 Agent 带教 | 安全感：有认证者、有归属 |
| 企业采购方 | Zeus 作为虚拟部门高并发承接流程；权限可审计、知识不流失 | 人效、留存、合规 |

## 4. 需求拆解（Epic → 需求 → 验收）

优先级：P0 = v1 必须；P1 = v1.x；P2/P3 = 后续版本。

### E1. 并发协同与决策内核（核心）

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E1.1 | 单意图扇出（fan-out）：按技能/域一次并行派发多个 Agent（含执行 Agent） | P0 | ✅ | 一层 fan-out/join 已落地（`Orchestrator` 按技能/显式名单并行派发、`Promise.allSettled`、部分失败/单路超时、failed/partial/needs-driver/completed 状态判定，见 design-fan-out.md）；**S3 完整 DAG 依赖编排已落地**（`src/orchestrator/dag.ts` 纯图函数：环/重复/未知依赖校验、拓扑分层、关键路径；`dag-runner.ts` 按波次并行、上游失败则下游跳过、needs-driver 冒泡、上游产物经 resolveParams 下传，6 项测试）；**H2 驾驶员入口于 2026-09-26 接好**（POST /api/intents 接受可选 `dag:{nodes}`，依赖编码在节点 `dependsOn`、`GET /api/intents/:id/dag` 回分层 + 关键路径，deferred #23 销项，全量 711 绿 / 74 文件） |
| E1.2 | 多流合并：多路 SSE/结果合并为操作者单视图，保持来源可辨 | P0 | ✅ | `mergeBranches` 分支内保序、分支间按选择顺序拼接，每事件带来源 vassal/taskId/runId；缺路按 branchTimeout/失败策略归为 partial/failed。服务端 SSE 推送合并流属 E5.5 |
| E1.3 | 多 Agent 协同决策：多方案生成、加权/投票/规则聚合为一个决策建议 | P0 | ✅ | ✅ 确定性规则聚合 unanimous/majority/weighted 输出各方立场/权重/理由，分裂时 conclusion:null 不臆断；**LLM-as-judge 对抗复核已落地**（`src/orchestrator/judge.ts`）：规则有结论且≥2 立场后，独立决策后端复核——高置信校准同意则背书、高置信校准分歧不静默覆盖也不放行弱多数，转 needs-driver 并开 `kind:judge-review` 冲突走 E6.2 操作者闭环；低置信/未校准 LLM 默认不采纳/后端故障只记录不动状态；与仲裁互斥（无结论走 arbitrate，同后端不自评）；judge 记录进 E1.6 回放时间线，14 项测试。多方案生成（noul 开放生成）仍未做 |
| E1.4 | 冲突检测与消解：识别 Agent 间结论冲突，给消解路径或升级操作者 | P0 | ✅ | `detectConflicts` 标记规则无法消解的多立场分裂，status=needs-driver 并经 onConflict 回调进监督台、不静默选边；**冲突裁决回写已闭环**（E6.2）；更丰富的自动消解规则 / LLM critic（S2）未做 |
| E1.5 | 并发治理：并发上限、任务队列、超时与取消传播、幂等 | P0 | ✅ | ✅ intentId 重放幂等（同键零出站）、cancelIntent 传播到全部非终态分支、单路超时、父子 runId 全链贯穿；**并发上限与有界队列已落地**（`src/orchestrator/semaphore.ts`：`maxConcurrentBranches` 进程内在途分支上界 + FIFO 等待 + `branchQueueLimit` 溢出即拒并把原因写进分支结果，租约在 finally 归还故失败不会卡死闸门；resumeBranch 走同一道门；不配上限=沿用无界行为）。`GET /api/metrics` 的 `queueDepth` 自此为真实值（此前恒 0）。**剩余归 deferred #9**：溢出时的执行 Agent 间分流/降级顺序（需 ≥3 真实执行 Agent 压测才定），PRD E4.10 同条 |
| E1.6 | 决策可追溯：每个决策可回放参与 Agent、输入、立场、聚合过程 | P0 | ✅ | FanOutResult 记录各分支、positions、decision.rule/reason/margin、sourced 事件与分支 runId；**独立离线回放器已落地**（`src/orchestrator/replay.ts`：纯只读函数按合并流重建确定性时间线，含输入/参与方/立场/聚合/冲突/后端仲裁/操作者决议，replaySnapshot 从持久化快照批量回放，renderReplay 出人读文本；记录损坏 fail-loud，8 项测试）；**已上操作者面**（v0.23）：`GET /api/intents/:id/replay` 出 JSON 时间线、`?format=text` 出人读文本，配对 `Orchestrator.getRequest()` 取回原始派发输入，存储记录无法回放时返回 500 而非半条时间线（tests/http-replay.test.ts 6 项） |
| E1.7 | 并发可观测：在途任务数、各 Agent 延迟/失败率、队列深度 | P1 | ✅ | 库内 `ConcurrencyMetrics`（`src/orchestrator/metrics.ts`）：在途数、并发峰值、队列深度、完成/失败/超时计数、各执行 Agent 延迟 min/max/avg/p50/p95 与失败率，经 Orchestrator 注入、`bootKernel` 默认装配（5 项测试）；**已随 H2 经 `GET /api/metrics` 暴露**。背压/有界队列落地前队列深度恒 0（deferred #9） |

**容量目标（本机 mock 基线已产出，真机待标定）**：单意图并发 Agent 数、平台同时在途任务数、P95 决策延迟的本机回环基线见 [capacity-baseline.md](capacity-baseline.md)（舒适扇出 ≤16、验证到 128 在途分支零丢失）；真机阈值在 ≥3 真实执行 Agent 压测（deferred #9）后定值。

### E2. Skill 技能体系（一等模块）

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E2.1 | Skill 显式声明：输入/输出、权限、依赖、版本 | P0 | ✅ | SkillSpec 独立于 Agent Card：id/name/version(major.minor.patch)/inputs/outputs/permissions(封闭 scope 词汇 realm·execute·network·credential·mcp)/dependencies；`validateSkillSpecShape` 纯校验，注册强制形状校验、依赖须已注册（拒前向引用）、拒绝自依赖与依赖环；SkillRegistry 已在 `bootKernel` 装配（执行 Agent card 经 onRegister 钩子自动导入）并随 KernelSnapshot 持久化重启恢复，见 tests/skills-validation.test.ts、tests/kernel-skills-state.test.ts |
| E2.2 | Skill 注册中心：登记、检索、版本化 | P0 | ✅ | `src/skills/registry.ts` SkillRegistry：id+version 唯一键、多版本共存、get 默认最新 active（显式版本可查 deprecated 供审计）、deprecate 标记不删、按名/域（findByDomain）/标签检索、registerFromCard 从卡片登记目录版本、resolveTeam 多技能组队（10 项测试）；**已上操作者面**（v0.23）：`GET /api/skills[?domain\|tag\|status]`、`GET /api/skills/:id[/versions]`、`POST /api/skills`（E2.1 显式规格注册，**逐字段白名单拷贝**，任意 payload 不能把未知键写进持久目录） |
| E2.3 | Skill 安装 / 加固 / 卸载断权 | P1 | ✅ | `install/uninstall/harden`（src/skills/registry.ts）：安装即 active 生效；卸载立即 status=uninstalled，resolveTeam 即刻显示 missing、默认查询不可见（无缓存授权）；加固只能收窄权限（bare scope 可降到 scope:action，拒越权授予）、约束叠加合并，随 spec 持久化；deprecated 不可重装，见 tests/skills-lifecycle.test.ts（8 项）；**已上操作者面**（v0.23）：`POST /api/skills/:id/{install,uninstall,deprecate,harden}`（加固越权即 400，不做静默授予；deprecated 重装 409）；**卸载断权已上派发路径**（v0.32）：`fanOut` auto 目标经 `SkillGovernor.activeProviders` 闸门（注册无 active 版本 → refused `skill-uninstalled` 不派发），卸载从此真夺权，见 tests/skill-governor.test.ts |
| E2.4 | 协同决策按 Skill 自动组队：一意图映射到所需技能并选 Agent | P0 | ✅ | registry 有 findBySkill；SkillRegistry.resolveTeam 对多技能返回每技能 slot（providers/missing/ambiguous）与 complete/missingSkills/ambiguousSkills，多候选标 ambiguous 绝不静默随机选（E2.2 测试覆盖）；**已上操作者面**（v0.23）：`POST /api/skills/team`（`body.skills` 非空数组，歧义槽位原样返回供操作者选边）；**auto 目标过注册中心闸门**（v0.32）：显式注册 spec 的 `providedBy` 收窄自动扇出目标（卡片自报技能从此只参与 catalogue 激活、不再逐字决定 auto 目标），无 active 提供者拒绝并审计，显式 `request.vassals` 是一等旁路，见 tests/skill-governor.test.ts |
| E2.5 | Mentor 传授 Skill 给新 Agent/员工 | P2 | ✅ | 新增 `src/skills/mentor.ts`：`MentorshipLedger` 可审计带教路径——commission 要求 mentor 已是该技能在册提供者（非提供者/未知技能/自教均拒，技能须 active）、teach 记录传授单元、assess 以**胜任力检查而非出勤**认证（required 硬门全过 + 加权分 ≥ 阈值，默认 0.8，可自定义）；认证通过才经 `SkillRegistry.grantProvider` 把学习者登记为新提供者（进 resolveTeam），评估失败不动提供者集合；dismiss 作废；台账随 KernelSnapshot `mentorships` 段持久化重启恢复。见 tests/mentor-transfer.test.ts（9 项）、tests/kernel-mentor-state.test.ts；**已上操作者面**（v0.23）：`GET/POST /api/mentorships` + `/:id/{lessons,assess,dismiss}`，**刻意不暴露 `grantProvider` 直调**——提供者身份只能经认证获得，否则"学习结果可验证"被 HTTP 面绕过（tests/http-skills.test.ts 共 14 项） |

### E3. Realm 数据域（数据主权底座）

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E3.1 | connect 目录生成 manifest（确定性 realmId、类型固定 personal/enterprise） | P0 | ✅ | realpath 派生 id 且幂等；未 connect 拒绝读写；类型不可变 |
| E3.2 | 扫描式 search + read | P0 | ✅ | 文本白名单/剪枝/1MiB 上限/symlink 拒绝；路径穿越双检拒绝 |
| E3.3 | contentDigest 基线 | P0 | ✅ | 全量指纹稳定（顺序无关），供备份与漂移对账 |
| E3.4 | Realm MCP server 正式暴露（streamable HTTP + 鉴权） | P1 | 🚧 stdio | stdio 壳已落地（root/connect 不出协议）；正式触发：read-realm 执行 Agent 出现 |
| E3.5 | write 与操作者授权凭证 | P1 | ✅ | **已收口**（`FsRealmStore.write` + `src/realm/grant.ts`：personal 默认可写、readOnly 连接拒写（即使持凭证）、enterprise 写须 `verifyDriverWriteGrant` 校验；tmp+rename 原子写、延续路径穿越/symlink/文本扩展名/1MiB 防护、写后 connect 快照与 `contentDigest`/itemCount 一致、审计回调记 `grantedBy`）。**2026-09-25 补上关键一环**：`connect` 不再拒 enterprise，且存储类型如实记录（此前 `realms.set` 硬编码 `personal`，企业域写闸门在真实 store 上永远走不到、只在纯函数测试里被 mock 打桩＝死代码）。**同日再补凭证本身（deferred #14 销项）**：凭证从"形状校验"变成**签名且一次性**——`issueDriverWriteGrant` 由内核铸 nonce、盖时间戳、用 RSK 的 Ed25519 签 JCS；`verifyDriverWriteGrant` 改 async，按 形状→realm 绑定→**验签**（`unsigned`/`unknown-key`/`bad-signature`）→有效期（含强制有效期，`no-expiry` 拒）判定；`DriverGrantLedger` 落盘前消费 nonce 且随内核快照持久化（`writeGrantNonces`，否则重启即重放）；`FsRealmStoreOptions.now` 注入时钟（此前测试只能用 `expiresAt: 2099-…` 伪装"仍然有效"）。运维面 `POST /api/realm/write-grants` + 日记写凭证透传（缺授权 **403**）+ 审计事件流 `driver-grant-issued`/`realm-write` + `GET /api/state` 报 `driverGrants.authority`。**仍属 P1 的半边**：MCP `tools/write` 暴露（其 actor 判定归 deferred #18）；enterprise 多租户分级是 E3.6，已另行落地 |
| E3.6 | enterprise Realm 多租户 | P1 | ✅ | **已落地**（`src/realm/tenant.ts` + `connect` 的 `tenant` 参数，设计见 [design-realm](design-realm.md) §7）：企业 Realm 挂 `{org, department?, member?}` 三级位置（写法 `acme/eng`，1-3 段，**段名逐字保留故非 ASCII 可用**、匹配按大小写折叠）；**个人域不是租户**——`connect(root,'personal',{tenant})` 抛错而非忽略，让"这是个人域"成为类型层面的陈述；同一 root 重连给不同 tenant 直接拒（不许静默改写已挂载数据域的边界），tenant 随 `RealmConnection` 进快照、**重启后仍带边界**（实测：第二次 boot 不传 realmRoots，恢复出的 realm 仍是 `acme/eng`）。层级是**结构性**边界：主体 scope 必须是 realm tenant 的前缀或相等（org 可下探部门；部门不能上望 org、不能旁视兄弟部门），**没有任何凭证能放宽它**；没声明租户的企业 Realm 只对操作者可达。env `ZEUS_REALM_ENTERPRISE="<root>::<tenant>"`（`::` 因 Windows 盘符已占单冒号；tenant 缺失/非法 → **拒启**）。测试见 `tests/realm-tenant.test.ts` 与 `tests/kernel-domains.test.ts`（**缺陷植入验证**：把 `tenantReaches` 改成恒真，跨部门/上望的拒绝用例在库层、真实目录解析层与 HTTP 层同时红 5 例） |
| E3.7 | 备份策略执行器 + 漂移检测 | P2 | ✅ | `src/vault/cli.ts` 零依赖执行器（`dist/vault/cli.js` / `npm run vault`）：`build`（密封 manifest-only 图）、`check`（L0 重连现盘对账 ok/changed/missing/unexpected，只读）、`backup`（密封 map+full bundle 两文件）、`restore`（digest 绑定校验后跨位写盘并复验）；密钥经 `ZEUS_VAULT_PASSPHRASE`（scrypt）或 `--key-file`（32 字节/hex），退出码 0 健康/1 错误/2 漂移/3 root 不可达供调度器判断；12 项 tests/vault-cli.test.ts + 编译产物全链路冒烟。**调度不内置**（design-vault §9：由外部 cron/systemd timer 触发，示例见 deployment.md §7） |
| E3.8 | 检索后端升级（倒排/向量） | P2 | ⬜ | SearchBackend 接口不变；阈值见 deferred #10 |

### E4. 执行 Agent 联邦（Vassal Protocol）

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E4.1 | Agent Card + fealty 注册（版本协商、健康探针） | P0 | ✅ | 无 fealty/版本不支持拒绝；字段与标准对齐 |
| E4.2 | tasks/send 与 sendSubscribe（JSON-RPC + SSE） | P0 | ✅ | 全链路状态机完整；SSE 缺终态快照即报错 |
| E4.3 | 结果回传 report-back（summary/evidence/cost/followUps） | P0 | ✅ | 四字段齐备；成本口径按 deferred #8 |
| E4.4 | 升级 escalation（input-required / 不可逆操作） | P0 | ✅ | 不可逆 execute 必须升级；带 approve/reject |
| E4.5 | 数据二极管与 dataPolicy 脱敏 | P0 | ✅ | 按 fealty 裁剪；越域即拒并审计 |
| E4.6 | 吊销强制力 | P0 | ✅ | 派发前阻断，不发请求/token；重复吊销幂等 |
| E4.7 | 全程审计（决策链 + 治理桥 + SLA 计时） | P0 | ✅ | 审计有序；sla.ackSeconds 违约单独决策。**v0.24 补上"落盘 + 可读"**：`bootKernel({ auditFile })` / `ZEUS_AUDIT_FILE` 把每条派发决策与治理事件追加为 JSONL（同时仍转发给调用方 sink），**并把吊销桥进审计**（此前 `bootKernel` 从未接 `onRevoke`，booted 内核里"吊销一个执行 Agent"不产生任何审计记录）；`GET /api/audit?runId\|vassal\|decision\|limit` 回读，按字节窗口只读尾部、记录不可解析时 500 而非给一条缩短的轨迹（tests/audit.test.ts + tests/http-audit.test.ts）。**v0.31 补上它最大的洞**：审计此前只在"能写下去"时才可信——按 `.env.example` 默认值起服务，sink 的 ENOENT 会逃出分支 `catch` 被重新标成"执行 Agent 失败"，**每条派发都带着磁盘错误的 reason 返回**。现在目录与文件在构造期建好（不可用即拒启并点名 `ZEUS_AUDIT_FILE`）、写出固定 0600 且轮转后重新收紧、运行期写失败**只计数不改变结果**，`GET /api/state` 报 `audit.failures/degraded`——轨迹断了看得见，而不是把锅甩给执行 Agent |
| E4.8 | 标准客户端守护（超集不破坏标准） | P0 | ✅ | 脚本已备（`scripts/acceptance-standard-a2a.mjs`，纯标准、不认 x-zeus-*）。**2026-09-25 纠偏：此前写的阻塞原因"真机待 pr-helper 部署"是错的——pr-helper 早已部署在生产并每天使用（`https://pr-helper-ten.vercel.app`，`vercel.json` 有 `/.well-known/agent-card.json → /api/a2a/agent-card` rewrite）。真正缺的是从来没人对线上执行过一次这条验收**（zeus 与 pr-helper 两边的记录里都没有）。关闭动作：`BASE_URL=https://pr-helper-ten.vercel.app node scripts/acceptance-standard-a2a.mjs`（默认 `deployment-health` 只读；线上若设 `ZEUS_A2A_TOKEN` 带 `TOKEN=`）。✅ **2026-09-25 首次对线上执行验收 PASS**（关闭）：`BASE_URL=https://pr-helper-ten.vercel.app node scripts/acceptance-standard-a2a.mjs`（走本机系统代理 `127.0.0.1:7897` + `NODE_USE_ENV_PROXY=1`）。跑通前修掉一处脚本失配：脚本原发 RPC 到 `/api/a2a/tasks`，线上 pr-helper 的 JSON-RPC 面在 `/api/a2a/agent-card`（GET=卡片、POST=RPC，`vercel.json` rewrite 证实），404 后修正端点。验收结果：agent card 发现（pr-helper、5 技能）+ `tasks/send` 被接受（task 生命周期启动，input-required）、exit 0。**v0.32 已接线出站认证**（`tokenFor` + seed `url\|token`），**M3 由此从"制品就绪"推进到"首执行 Agent 真机受调度"只剩 Zeus↔loom 联调 + 起实例注册线上执行 Agent 扇出** |
| E4.9 | fealty 签名链 v1（生产 RSK + R1/R2 接线） | P1 | 🚧 | 纯函数 + H1 public 封签 + **R1 internal 封签（v1.1：active\|revoked 两态 attestation，含吊销行的 internal 快照可封签离线验）均已接**；**v1.2（2026-09-25）补版本闸**：载荷加 `schemaVersion`（落在 `seal.snapshotDigest` 覆盖的对象内，故不必改签名输入即被签名保护），并把 `schemaVersion`/`seal.v`/`attestation.v` 三道闸提到验签最前面、拒绝原因点名是哪道——原先 `v: 1` 自 v1 就写入但**验签从不读**，属装饰性字段；缺 `schemaVersion` 的旧件读作 1 仍可验签（deferred #22 销项，契约见 design-fealty-signing §4.1）；**离线验签从此有命令行入口**（2026-09-26 `scripts/verify-roster.mjs` / `npm run verify:roster`：只持公钥即可判名册真伪，deployment §6 那条"封签经独立通道验签通过"从不可执行变为可执行）；销项仍须生产 RSK 密钥托管/轮换与 R2 bayjf 客户端公钥验签、过八条验收（deferred #7，触发=bayjf 公开前） |
| E4.10 | 执行 Agent 背压降级顺序 | P2 | ⬜ | ≥3 执行 Agent 在线压测（deferred #9） |

### E5. HTTP 门面与名册

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E5.1 | roster internal/public 双投影 | P0 | ✅ | public 裁掉端点/探针/已吊销；确定性排序 |
| E5.2 | H1：/healthz、/api/roster/public（seal）、/api/roster（bearer） | P0 | ✅ | public 与 **internal 双份快照均封签、离线可验**（签名链 v1.1：attestation 扩 `active\|revoked` 两态，internal 含 revoked 行各自封签；revoked attestation 证永久吊销事实、不带硬过期，快照整体新鲜度由 seal maxAge 绑定；验签要求 attestation 状态与条目**精确匹配**，缺 source / 状态矛盾 fail-loud，仅 active 校验硬过期）；internal 响应 `Cache-Control: no-store`、bearer 常量时间比对、未配 token 整组不挂载；条目/provenance/签名三类篡改与过期均拒绝 |
| E5.3 | 持久化注册表（替换实例内存） | P1 | ✅ | `src/state/kernel-state.ts`：执行 Agent 注册表（含已吊销）、监督台升级队列（重建 task/conflict 幂等索引）、编排意图结果与原始请求（重启后幂等重放、resumeBranch 可用）经 `FileKernelStateStore` 原子落盘（tmp+rename）与版本校验恢复；已接入 `bootKernel` 启动装配（`ZEUS_STATE_FILE` 启动恢复、SIGINT/SIGTERM 原子落盘）。**Realm 连接状态已纳入快照并重启自动 reconnect（G4 ✅）**；metrics 运行态不持久化 |
| E5.4 | bayjf R2 验签公开签名目录 | P1 | ⬜ | 只消费 seal 快照并客户端验签；闸门 = E4.9。**2026-09-26 起"客户端验签"这一侧的工具已在库内**（`npm run verify:roster` 用出货实现做完整验链 + 卡片深比对），本条剩余的是**一个真正的外部消费方**接上它，不是再写一份验证逻辑 |
| E5.5 | H2 操作者 API + 服务端 SSE（含多 Agent 合并流） | P1 | ✅ | **H2 操作者 API**（bearer 保护，未配 `ZEUS_INTERNAL_TOKEN` 整组不挂载）：`POST /api/intents` 扇出、`GET /api/intents/:id` 回查、`POST /api/intents/:id/cancel`、`GET /api/escalations` + `POST .../:id/approve\|reject\|resolve`（resolve 把裁决立场回写聚合决策，打通 E6.2）、`GET /api/metrics`；端到端测试 `tests/http-h2.test.ts` + 进程级冒烟。**H3 服务端 SSE 已落地**：`GET /api/intents/:id/events` 经 `ProgressHub` 实时推送 branch-started/branch-ended/intent-finished（15s keepalive、已完成意图回放、flushHeaders），见 tests/http-sse.test.ts。**执行 Agent 上线入口已接**：`POST/DELETE /api/vassals` + `ZEUS_VASSAL_SEEDS`（G1）。**v0.23 补面（"内核有、操作者看不见"四类一次接上，全部 bearer 保护、未配 token 整组不挂载）**：`/api/memory/{events,facts,recall,retractions,integrity}` + `POST /api/memory/{retract,forget-subject}`、`/api/skills*` 与 `/api/mentorships*`、`GET /api/org/accountability/:intentId`、`GET /api/intents/:id/replay`。**v0.24 再补三面**：`/api/connectors*`（E7 声明/读/连接/吊销，**响应一律脱敏 token，只报 `hasToken`**；握手失败 502 而非 400）、`GET /api/decision`（进程实际解析到的决策后端与 judge 闸门，此前只在 stderr 一行）、`GET /api/audit`（E4.7 审计回读，按 runId/vassal/decision 过滤，**尾部按字节窗口读**、记录损坏报 500 不给缩短的轨迹）；Realm/Vault 仍不上 HTTP（design-realm §6.1 唯一传输 MCP、design-vault §9 CLI+外部调度）。**v0.26 再补两面**：`GET /api/escalations?kind=`（`task-input`/`intent-conflict`/`memory-dispute` 分流——缺参与记忆争议要的回答完全不同，此前混在一列；`OversightDesk.list(status, kind)` 支持组合过滤）、`GET /api/state`（内核盘点：persistence 是否启用/状态文件/是否从快照恢复 + audit 上限 + 执行 Agent/升级/意图/技能/连接器/带教/部门/记忆事件与事实/Realm 的**计数**；由 `src/state/stats.ts` 的 `kernelStats(kernel)` 组装，**只报计数与路径、绝不服用快照内容**——里面是连接器 token 与用户记忆明文）。**v0.28 加 Realm 治理面**（边界见 design-realm §6.4：内容仍只经 MCP，HTTP 只到元数据与授权为止）：`/api/domains*`（挂载与租户级、授权签发/吊销、`?access` 干跑探针）与 `POST /api/intents` 的 `realmSource`（内核代取内容并核对"声明的域 vs 真实的域"，见 E6.4）。**v0.29 再补上岗面**：`GET /api/org/departments/:id/commissions`（台账 + 每份实时 verdict）、`POST …/commissions`（开档）、`…/waive`、`…/commission`（签字）、`…/withdraw`、`GET …/briefing/:agentId`（首日简报）、`POST …/first-task`（过门后真派发，见 E9.2） |

### E6. 监督台与操作者

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E6.1 | OversightDesk：升级队列 + approve/reject + reject 联动 cancel | P0 | ✅ | taskId 幂等；只受非终态；全程审计 |
| E6.2 | 多 Agent 冲突升级与裁决 | P0 | ✅ | 冲突经 onConflict→`conflictsToDesk`→OversightDesk.ingestConflict 入队（按 intentId 幂等）；decideConflict 校验立场后，Orchestrator.resolveIntent 经纯函数 applyConflictResolution 把操作者立场回写聚合决策、清空冲突、重算状态、记 driverResolution（6 项测试） |
| E6.3 | approve 后补参自动重派 | P1 | ✅ | 库内 `Orchestrator.resumeBranch` 单分支带合并参数重派、整意图重算（仍 needs-driver 再升级）；冲突裁决回写经 `POST /api/escalations/:id/resolve`（E6.2）；**一键 HTTP 闭环已接线（G6）**：`POST /api/escalations/:id/approve-resume` approve 携带 params 后经 `findIntentForBranchRun` 定位意图自动 resumeBranch，见 tests/http-approve-resume.test.ts |
| E6.4 | 双域共存授权界面 | P2 | ✅ | **已落地**（设计见 [design-realm](design-realm.md) §7.2–§7.5，deferred #6 销项）。**授权粒度**：`DomainGrant`（subject × realmId × access × grantedBy × expiresAt × nonce）只管**域边界**（个人域→企业域需显式授权；**企业域→个人域永远拒，连能表达它的凭证形状都不存在**），与 E3.5 的 `DriverWriteGrant` 分层不合并（前者"这主体能不能碰这域"、后者"这一次企业写被授权了"）；`decideRealmAccess()` 是**唯一**判定函数（派发取数与操作者探针共用，reason 精确到 `grant-expired`/`access-not-granted`/`no-grant`，因为这三者要求操作员做不同的事）；`DomainGrantRegistry` 令 **nonce 一次性**（吊销后仍算已用，防"撤销"变成重放入口）且随 KernelSnapshot 持久化（重启既不静默放宽也不静默丢失）。**取数与审计呈现**：新增 `resolveRealmSource()` —— 此前 `realmHits` 由调用方自报、内核从未自己进过任何 Realm，故"声明 personal 域却塞企业域内容"无从发现；现在内核按 realmId 自己取、核对**真实 type 与声明是否一致**（不符 `realm-type-mismatch` 拒）、过边界判定后才把命中连同 realmId 送进派发，**放行与拒绝都落同一根审计事件流**（`domain-read`/`domain-refused`，reason 前缀）。**界面（无 UI 产品的运维面）**：`GET /api/domains`（挂载/租户级/只读/条目数/指纹，**绝不含绝对路径**）、`POST /api/domains/grants`、`DELETE /api/domains/grants/:id`、`GET /api/domains/access`（**干跑探针**，不读任何内容）、`GET /api/audit?decision=domain-refused`；`GET /api/state` 增 `enterpriseRealms`/`tenantScopedRealms`/`domainGrants`（"没标租户的企业域数"是操作员最该一眼看到的风险位）。测试 `tests/realm-authorization.test.ts`、`tests/realm-source.test.ts`、`tests/http-domains.test.ts`；植入缺陷验证：去掉企业域→个人域的拒绝分支 → 3 例红；关掉声明核对 → 2 例红 |

### E7. MCP 连接器（三条能力接入通道·连接世界）

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E7.1 | 基于 MCP 的外部系统接入（resources/tools/prompts） | P1 | ✅ | 新增 `src/mcp`：`McpClient` 零 SDK 实现 streamable-HTTP JSON-RPC（initialize 握手 + initialized 通知，兼容 application/json 与 text/event-stream 两种响应），tools/resources/list 发现能力；`ConnectorRegistry.connect` 执行握手，服务器不可达拒绝并审计（refused），`revoke` 立即移出活动集合，见 tests/mcp-connectors.test.ts（11 项）；**tools/call 调用面已接**（v0.32）：`McpClient.callTool(name, args)` + `ConnectorRegistry.callTool(id, name, args)`——**能力边界=握手发现且经声明 `permissions` 裁剪后的清单**（未连接/已吊销/不在清单内一律拒绝），HTTP 面 `POST /api/connectors/:id/tools/:name/call`（上游失败 502），见 tests/http-connectors.test.ts（12 项） |
| E7.2 | 连接器登记与最小权限声明 | P1 | ✅ | `declare` 强制封闭权限词汇（validatePermissionClaims）、重复声明拒绝；连接后只暴露声明边界内工具（bare `mcp` 全放行，`mcp:<tool>` 精确授权，边界外工具不出现）；连接器声明经 bootKernel 装配并随 KernelSnapshot `connectors` 段持久化恢复（连接不自动重建立）；**操作者面已接**（v0.24）：`GET /api/connectors[?status]`、`GET /api/connectors/:id`、`POST /api/connectors`（声明，可带 token）、`POST /api/connectors/:id/connect`（真跑 MCP 握手并按权限边界裁剪工具）、`DELETE /api/connectors/:id`（吊销即时生效）；**所有响应脱敏——只报 `hasToken`，绝不回显 token**（tests/http-connectors.test.ts 用真 HTTP MCP stub 断言响应体不含密钥）。附带：`GET /api/decision` 报进程实际用的决策后端与 judge 闸门（此前只有 stderr 一行）；**调用路由**（v0.32）：`POST /api/connectors/:id/tools/:name/call`（body.arguments 校验、边界外工具 400、未知连接器 404、上游失败 502）——被裁剪的能力清单从此有消费者 |

### E8. 个人情感层

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E8.1 | Map 备份清单：加密 manifest + 恢复协议 | P1 | ✅ | `src/vault`：buildVault 出图，marks 只存 itemId + sha256 指纹（**正文零泄漏**有断言）；seal/open 走 AES-256-GCM（scrypt 口令派生或 raw 32-byte key），密钥与图分离；原地 restoreDryRun 重连校验。**v0.30（deferred #13）**：图的 `realm` 字段换成可判别的 `source`（`realm` \| `files` 白名单），L0 的现盘视图从 `RealmStore` 解耦成 `LiveSource` 端口，**内核状态文件 kernel.json 从此可备份可恢复**（它不在任何 Realm 里）；v1 图读入归一化、写出不兼容。**按图真实恢复走通**，见 tests/vault.test.ts（20 项） |
| E8.2 | 备份清单备份与备份思路 | P1 | ✅ | 图经 seal 加密静态保存、可复制；packFull 产出 manifest-only / full 两档（full 带加密内容包）；restoreFromBundle 在目录被清空或换新位置时写盘恢复并复验，恢复流程经测试演练。自动/异地/云备份为非目标（design-vault §9） |
| E8.3 | Diary 日记：记忆叙事化备份 | P2 | ✅ | **已落地**（`src/diary`，[design-diary](design-diary.md) v0.1）：buildDiary 纯函数按日历天（UTC/IANA 时区）分桶、确定性排序、renderEventContent 只呈现不臆造（string / ClaimContent / 对象 / 空内容四态、超长截断标注）、可选纳入事实、算内容 digest；每行锚 eventId、provenance 覆盖全部来源，混 realm 拒绝；persistDiary 经 Realm.write 写 `diary/YYYY-MM-DD.md`（幂等）、exportDiary 稳定 JSON；buildDiariesFromState 按 realm 分组、按需（realm/date）构建；**操作者 HTTP**：GET /api/diary（按 realm/date 读，缺日 404）、POST /api/diary/generate（经 Realm.write 落 `diary/YYYY-MM-DD.md`，无可写 realm 409、无事件 400）。25 项测试（diary 19 + http-diary 6）含真实 realm 往返 |
| E8.4 | 传承（dead-man switch + 密钥托管） | P3 | ⬜ | 触发可靠、可撤销；法律框架齐备（deferred #2/#3） |
| E8.5 | 记忆整理与遗忘权（事件→事实、混合检索、擦除、漂移对账） | P1 | ✅ | **补记行**：能力在 v0.8–v0.12 已落地并验收，此前只进演进日志、PRD 无对应需求行。`src/memory`（[design-memory-consolidation](design-memory-consolidation.md) v0.3）：append-only 事件日志，**事实无公开写入口**（只经纯函数 `consolidate` 产出、逐条带 provenance）、观察去重累积、矛盾默认 disputed 并确定性升级进监督台、置信度按可靠度加权（指标 failureRate 派生 + 操作者纠错 −0.15/次）、跨 realm 读写拒绝并审计、沿 runId 离线回放；P2 三项齐（BM25+向量混合检索与可重建派生索引、`retractFacts`/`forgetSubject` 遗忘权 + tombstone、`reconcileMemoryStates`/`verifyMemoryState` 漂移对账与横切不变量）；随 KernelSnapshot 持久化重启恢复。**v0.23 上操作者面**：`/api/memory/{events,facts,recall,retractions,integrity}` + `POST /api/memory/{retract,forget-subject}`（11 项 tests/http-memory.test.ts；本面只有读与擦除，没有事实写入口） |

### E9. 企业组织层

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E9.1 | Mentor Agent：文化/业务/职责带教 | P2 | ✅ | **已落地**（`src/onboarding/briefing.ts`，设计见 [design-onboarding](design-onboarding.md) §4）：`composeBriefing` 只从已有事实装配岗位上下文——部门与座位/责任链（E9.3）、已认证技能与还缺项（E2.5 `MentorshipLedger` 的 certified 记录）、岗位技能·同事·**谁能教这项缺的技能**（`SkillRegistry` + 名册 + `findBySkill`）、组织惯例（**本部门 realm 的记忆召回**，跨域读直接抛错）、边界（realm 类型/租户/只读/条目数 + 读写两向判定）。**两个难看的真相刻意留下**：`culture.totalFacts` 与命中数分开报，所以"组织什么都没写"不等于"写了但与本岗位无关"；`canWrite` 只表示域边界允许写，企业写入另需 `DriverWriteGrant`（`writeNeedsGrant` 单列）。简报带**去时间戳的稳定 digest**，首日任务因此能说明自己是照哪一版简报派出去的。真机侧仍差一项：由**真实执行 Agent**当 Mentor 完成一次带教并通过（与 E4.8 同一批） |
| E9.2 | 上岗即用流程 | P2 | ✅ | **已落地**（`src/onboarding/commission.ts`，设计见 [design-onboarding](design-onboarding.md) §2–§5）：**四道门现算不缓存**——seat（部门名册 E9.3）/ account（在册且未吊销的执行 Agent）/ authorization（`decideRealmAccess`：E3.6 租户 + E6.4 授权）/ mentorship（E2.5 认证，**出勤不算**）。记录只存决定（谁开档、谁签字、谁豁免了什么），资格每次重算：**先签字、之后吊销执行 Agent 或撤出名册 → 资格自动失效**，不依赖任何人记得去改一个状态位。三条严格化：`requiredSkills` 为空**必须显式豁免并写理由**（"没人说这岗位要会什么"是一个发现，不是一次放行）、豁免不得覆盖已声明的要求、座位租户不得比其 realm 更宽。`POST /api/org/departments/:id/first-task` **真走一次 fanOut**：realm 取该域连接现状的真实类型、目标限定该 Agent、`params.onboarding` 带 commissionId 与简报 digest；未签字 → 409 `commission_gate` + `blockedOn`。账号→授权→Mentor→上岗→首日任务已在真实进程跑通（见 handoff Active work 44 的冒烟记录）。门的可失效性做过三次缺陷植入：门常真 → 2 例红；只信记录不重算 → 3 例红；能力门常过 → 2 例红 |
| E9.3 | 虚拟部门编制（Team）与结果责任 | P2 | ✅ | **已落地**（`src/org`，[design-org](design-org.md) v0.1）：Department 不可变、单 lead、成员按 agentId 唯一（createDepartment/assignMember/removeMember/setLead）；buildOrgChart/renderOrgMarkdown 编制可视；traceAccountability 把真实 FanOutResult 追到执行 Agent、部门 lead（去重）、裁决 driver，无编制 Agent 标 unassigned 不丢弃；**OrgRegistry** 有状态持有编制、纳入 KernelSnapshot 随 boot 持久化（重启不丢）；**操作者 HTTP**：GET /api/org/chart、POST /api/org/departments、POST /api/org/departments/:id/members（建编/安置 lead·member，未知部门 404、重复 409）、**v0.27 补全编制可管理性**：`POST /api/org/departments/:id/lead`（换 lead，原 lead 降为 member 不被丢弃）、`DELETE /api/org/departments/:id/members/:agentId`（撤岗——此前**只能加不能减**，操作者能扩张部门却纠正不了它），两者对不在编制内的 Agent 返回 404。**同一批修掉一个功能阻断**：`departmentId = dept:<ascii slug>` 使中文名（如"研发部"）在任何入口都建不起来、且报错读起来像输入有误；现无 ASCII 可提取时落到 `dept:<sha256(name) 前 8 位>`（确定性、URL 安全、同名同 id、异名异 id），显示名逐字保留，ASCII 名的 id 形态不变。29 项测试（org 15 + org-registry 8 + http-org 6）；**责任链已上操作者面**（v0.23）：`GET /api/org/accountability/:intentId` 把已存意图投影为 执行 Agent → 部门 lead → 裁决操作者（+5 项 http-org-accountability；需 orgRegistry 与 orchestrator 同时装配才挂载） |
| E9.4 | 外部 Agent 信任分级与沙箱 | P2 | ⬜ | 首个矩阵外 Agent 接入前完成（deferred #5） |

### E10. 平台工程

| ID | 需求 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| E10.1 | CI（typecheck/build/test 矩阵） | P0 | ✅ | `.github/workflows/ci.yml`（**Node 22/24 矩阵**，npm ci → typecheck → build → test，零 secret）。**矩阵于 2026-09-26 由 20/22 换为 22/24**（deferred #15 销项，选项 B）：`package.json` 加 `engines.node: ">=22.0.0"`、新增 `.npmrc` 的 `engine-strict=true`（无它则 engines 仅警告、exit 0）、`.nvmrc` 写 22；依据为三档实测（20.20.2/22.23.1/24.20.0 各 704 绿 + 22/24 真进程冒烟 200）与上游发布事实（v20 最后一次发布 2026-03-24）。**顺序于 2026-09-26 由 typecheck→test→build 改为 build 在 test 之前**：`tests/verify-roster.test.ts` 是第一个**要跑编译产物**的套件（它 spawn 的脚本 `import('../dist/registry/signing.js')`，用出货实现而不是另写一份验签），旧顺序下 CI 上 `dist/` 还不存在，实测把 `dist` 移走即 **7 例红**。**2026-09-25 首次真机确认**：dev 分支 push `206af94..3e91a9a` 触发 run 36024156638，两个矩阵各约 28s **全绿**（472/472，无 flaky 复现）——此前各批次只有"本地按 CI 序列实跑全绿"，GitHub 侧结果一直未观测。遗留注解（checkout@v4/setup-node@v4 target Node 20 被强制跑在 24、ubuntu-latest 2026-10-19 迁移）登记 deferred #12（action 版本部分已随 `@v7` 升级销项）。**同日复验三次全绿，CI 已是例行防线而非待办关口**：随后两批 push 分别由 run 36045209815（512/60 文件，30s/28s）与 run 36061395015（**532/61 文件**，26s/22s）在 Node 20.x / 22.x 双矩阵验过，无 flaky 复现；`ubuntu-latest` 2026-10-19 自动迁移那条 runner 层风险另登记 **deferred #16**；**该条已于 2026-09-26 销项**：`runs-on` 由 `ubuntu-latest` 钉为 `ubuntu-24.04`（Node 矩阵**仍未动**，归 deferred **#15**：同日已备提案——20/22/24 三档实测各 704 全绿、22/24 真进程冒烟 200、`engines` 不配 `engine-strict` 仅为警告，见登记项） |
| E10.2 | 真机部署：pr-helper + Zeus 门面 | P1 | ⬜ | 支撑 E4.8 真机验收与 Zeus↔loom 联调 |
| E10.3 | 库公共入口与构建产物 | P0 | ✅ | src/index.ts 聚合导出；dist 含 .d.ts |
| E10.4 | 并发压测与容量基线 | P1 | ✅ | **本机 mock 回环基线 v0.3**（[capacity-baseline.md](capacity-baseline.md)，`npm run bench:capacity`），五场景：A 扇出宽度、B 并发意图、**C H2 门面全链路（真实 `app.listen` 回环 TCP + bearer + JSON，128 分支点门面附加仅约 5–10ms 墙钟、吞吐约低 10–15%，低并发几乎无差）**、**D 高并发取消传播（128 个 input-required 挂起分支 14–18ms 全部取消、mock farm 实收 240 个 tasks/cancel 零丢失零重复）**；单意图扇出 ≤16 墙钟≈单执行 Agent（内核附加 5–15ms）、128 在途分支零丢失、metrics 峰值准确、**E 并发闸门代价（v0.3，`--cap` 显式开启：128 分支负载下 cap 无限制/32/16/8 —— 实测 max in-flight 恰等于 cap、peak queue 恰等于 128−cap、128 分支零丢失；吞吐随 cap 近线性、尾延迟≈墙钟；绝对值跨次散布 ±2×，故只用比值与不变量下结论）**；附带把 `branchTimeoutMs` 从 20×think 提到 40×think 并注明它是防卡死看门狗而非 SLO（满载时对连续尾段误触发）。**口径限制**：D 取消的是已 settle 的非终态（input-required）分支，对仍 working、fanOut 未返回的进行中意图尚无按 intentId 的一等取消；真机容量（LLM/网络）待 ≥3 真实执行 Agent + deferred #9 有界队列后按同法重测 |

## 5. 里程碑（建议）

| 里程碑 | 内容 | 出口标准 |
|---|---|---|
| M1 内核基座（当前） | E3.1–3.3、E4.1–4.7、E5.1–5.2、E6.1、E10.1/10.3 | 98 测试绿；治理闭环库内可验证 |
| M2 并发决策内核 | E1.1–1.6、E2.1/2.2/2.4、E10.4 | 单意图多 Agent 并发+聚合+冲突升级可演示；容量有基线 |
| M3 真机闭环 | E4.8、E10.2、Zeus↔loom 联调、E5.3 | 两执行 Agent 真机受调度；重启状态不丢。**2026-09-25：E4.8 首次真机验收 PASS（关闭）**，docker 实构实跑 PASS（build/keygen/run/health/probe）；剩 **Zeus↔loom 联调** + "起一个 Zeus 实例注册线上执行 Agent 做一次真机扇出"（pr-helper 可作第一执行 Agent，loom 为第二执行 Agent） |
| M4 Realm 开放 | E3.4/3.5、E4.9、E5.4 | 标准 MCP client 可读 Realm；bayjf 公开验签名册 |
| M5 情感与成长 | E8.1–8.3、E2.3、E7 | 备份清单恢复演练通过；Skill 可安装/传授 |
| M6 企业/传承 | E9、E8.4 | 员工上岗即用；继承协议端到端（P3） |

## 6. 成功指标（待基线）

- **并发决策（核心）**：单意图可并发 Agent 数、同时在途任务数、P95 决策延迟、多 Agent 决策与单 Agent 的质量对比、冲突被正确识别率。
- 信任：备份清单恢复成功率（目标 100%）；数据事故数（目标 0）；吊销生效延迟（目标实时）。
- 个人：周活留存、备份覆盖率、情感闭环验证用户数。
- 企业：新员工上岗耗时、流程 A2A 承接比例、知识可检索率；SLA 受理违约率。

## 7. 演进日志

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-09-22 | 首版：9 Epic、~40 需求、里程碑与成功指标 |
| v0.2 | 2026-09-22 | 核心定位修正为"高并发多 Agent 协同决策平台"：新增 E1 并发协同与决策内核；Skill 提升为独立 E2 并前置；新增容量目标、并发指标与 M2 里程碑，后续 Epic 顺延 |
| v0.3 | 2026-09-22 | M2 第一批落地：E1.1 一层 fan-out/join、E1.2 多流合并、E1.5 幂等/cancel 传播/单路超时、E1.3 规则聚合、E1.4 冲突升级（库内，design-fan-out.md，26 项测试）；E1 状态由 ⬜/🚧 更新 |
| v0.4 | 2026-09-22 | MVP 评审后六切片批次：E2.2 Skill 注册中心 ✅、E2.4 组队解析 ✅、E1.1 S3 完整 DAG ✅、E1.4/E6.2 冲突裁决回写 ✅、E6.3 补参重派骨架 🚧、E1.7 库内并发指标 🚧、E5.3 内核状态落盘恢复 🚧；另落地模型无关决策后端 src/decision（Jev/LLM 适配器，见 design-decision-backend v0.2）；全量 166 测试绿（22 文件） |
| v0.5 | 2026-09-22 | H2 操作者 API 落地：E5.5 升 🚧（意图扇出/回查/取消、升级队列 list/approve/reject/resolve 决议回写、GET /api/metrics，bearer 保护，12 项端到端测试 + 进程级冒烟）；E1.7 升 ✅（指标经 HTTP 暴露）；E5.3 已接启动装配；修复服务端生成 intentId 的意图不落表导致无法回查/决议的缺口；全量 199 测试绿（26 文件） |
| v0.6 | 2026-09-22 | A 批次收口四个缺口：**G1 执行 Agent 上线入口**（`POST/DELETE /api/vassals` + `ZEUS_VASSAL_SEEDS`）、**G4 Realm 连接持久化**（snapshot 增 realms、重启 reconnect、`ZEUS_REALM_ROOTS`）、**G6 E6.3 一键补参重派**（`POST .../approve-resume` + `findIntentForBranchRun`）、**G5 H3 SSE**（`GET /api/intents/:id/events` + `ProgressHub`，branch 生命周期实时事件）；E5.3/E5.5/E6.3 升 ✅；全量 217 测试绿（31 文件） |
| v0.7 | 2026-09-22 | E2.1 Skill 显式规格落地：SkillSpec 独立于卡片（version/inputs/outputs/permissions 封闭 scope/dependencies），注册强制形状校验、依赖须已注册、拒自依赖与依赖环（validate-spec 纯函数）；SkillRegistry 接入 bootKernel（onRegister 自动导入 card skills）并随快照持久化恢复；E2.1 升 ✅；全量 229 测试绿（33 文件） |
| v0.8 | 2026-09-22 | 记忆整理协议 P0 落地（design-memory-consolidation §7）：新增 `src/memory`——append-only 事件日志、事实无公开写入口（仅经纯函数 `consolidate`）、同事实观察去重合并累积 provenance、矛盾默认 disputed + 可确定性生成的 escalation（later-and-more-reliable 规则方可 supersede）、置信度按 Agent 历史可靠度加权（同源重复不增强、独立来源印证小幅提升）、跨 realm 读写拒绝并审计、沿 runId 离线回放事件与事实；八条首版验收逐条覆盖，11 项测试；全量 240 绿（34 文件） |
| v0.9 | 2026-09-22 | 记忆 P1 落地：事件日志与事实纳入 KernelSnapshot（`memory` 段，重启完整恢复）；FanOutRequest/Result 与 intent-finished 事件贯通 `realmId`；意图进入终态时自动整理对应 realm（可靠度从并发指标 failureRate 派生，无记录默认 0.5），矛盾经新增 `memory-dispute` 升级类型幂等进 OversightDesk；`POST /api/intents` 透传 realmId；3 项测试；全量 243 绿（35 文件） |
| v0.10 | 2026-09-22 | 三块收口：**可靠度纠错回写**（OversightDesk onDecided 钩子，操作者裁决 memory-dispute 后对败诉作者记 correction，每次 −0.15，随快照持久化）、**E2.3 Skill 生命周期**（install/uninstall/harden，卸载即时断权、加固只能收窄）、**E7 MCP 连接器**（零 SDK JSON-RPC client + 连接器登记/最小权限/发现/吊销，声明随快照持久化）；E2.3/E7 升 ✅；新增 17 项测试，全量 260 绿（37 文件） |
| v0.11 | 2026-09-22 | 记忆 P2 落地：新增 `src/memory/recall.ts`——混合检索（BM25 词法 + 向量余弦，alpha 可调，默认本地确定性 signed-hashing embedder、`Embedder` 端口可注入同域模型），召回索引为不持久化的可重建派生物，仅 active/disputed 入索引；遗忘权（`retractFacts` 即时摘除索引、`forgetSubject` 按主体抹除，tombstone 台账随快照持久化，事件日志 append-only 保留供治理回放）；11 项测试；全量 271 绿（38 文件） |
| v0.12 | 2026-09-22 | 记忆 P2 漂移对账落地：新增 `src/memory/reconcile.ts`——`reconcileMemoryStates` 两时点快照逐字段 diff（事件增删、事实 added/removed/changed、correction/tombstone 增量）、`verifyMemoryState` 横切不变量校验（factId 可重算、provenance 可解析且不跨域、retract↔tombstone 一一配对、事实不重复），`MemoryStore.verifyIntegrity` 便捷入口；factId 计算导出复用；9 项测试；P2 三项齐；全量 280 绿（39 文件） |
| v0.13 | 2026-09-22 | E2.5 Mentor 传授落地：新增 `src/skills/mentor.ts` `MentorshipLedger`（commission 须在册提供者、teach 记录、assess 胜任力硬门+加权阈值、认证才 grantProvider、dismiss）；SkillRegistry 增 isProvider/grantProvider；台账经 KernelSnapshot `mentorships` 段持久化；E2.5 升 ✅；新增 10 项测试；全量 290 绿（41 文件） |
| v0.14 | 2026-09-23 | 新增架构立场文档 [design-agentic-integration](design-agentic-integration.md) v0.4（本文件不复制全文）：Agent 是新的编排/集成层而非替代 REST，两层形态=智能层（协商/非确定）压在原语层（契约/确定/可回放）之上；边界收口为「结构化意图→确定性闸门」，按可逆性/确定性需求/可验证性/爆炸半径四轴划分；行业现状五种主流实践与 L1 连接/L2 工具设计/L3 护栏三层成熟度；明确 Zeus 定位为 **AI 原生多 Agent 团队运行时**，目标软件的 Agent 可用成熟度决定团队如何调用。无代码变更 |
| v0.15 | 2026-09-23 | Vault 备份清单与恢复协议落地（[design-vault](design-vault.md) v0.1）：新增 `src/vault`——buildVault 出图只存引用+逐 item 指纹（正文零泄漏）、AES-256-GCM seal/open（scrypt/raw key，密钥分离）、L0 原地 restoreDryRun 重连校验与漂移检测、L1 packFull 加密内容包 + restoreFromBundle 跨位写盘恢复（FsRestoreSink）；Realm 增只读 `entries()` 枚举；E8.1/E8.2 升 ✅、E3.7 升 🚧；新增 20 项测试，全量 310 绿（42 文件） |
| v0.16 | 2026-09-23 | E3.7 备份策略执行器落地：新增 `src/vault/cli.ts` 零依赖 CLI（build/check/backup/restore，密钥经口令 env 或 key-file，退出码 0/1/2/3），`npm run vault` 入口；deployment.md 增 §7 备份恢复与 cron 示例（调度不内置，design-vault §9 边界不变）；12 项 CLI 测试 + 编译产物全链路冒烟（出图→篡改 exit 2→全包→销毁→跨位恢复内容一致→错钥 exit 1）；E3.7 升 ✅；全量 322 绿（43 文件） |
| v0.17 | 2026-09-24 | **E1.6 离线决策回放器**（`src/orchestrator/replay.ts`：replayDecision/replayDecisions/replaySnapshot/renderReplay，纯离线只读、确定性时间线、损坏 fail-loud，8 项测试）升 ✅；**E10.4 本机容量基线**（`scripts/bench-capacity.mjs` + `npm run bench:capacity` + docs/capacity-baseline.md：真实回环 HTTP mock 执行 Agent 群，扇出宽度/并发意图两场景，≤16 扇出墙钟≈单执行 Agent、128 在途分支零丢失，明确 mock 近似与真机重测触发条件）升 ✅；全量 330 绿（44 文件） |
| v0.18 | 2026-09-24 | **E1.3 LLM-as-judge 对抗复核接决策后端**：新增 `src/orchestrator/judge.ts` `judgeDecision` 纯函数（规则有结论后独立复核：同意背书、高置信分歧升级 judge-review 冲突走操作者闭环、低置信/未校准/故障只记录），OrchestratorOptions 增 judge* 开关组（默认关），fanOut/resumeBranch 接线，与 S2 仲裁互斥；Conflict 增 `kind:split\|judge-review`、FanOutResult 增 `judgeReview`；E1.6 回放器增 judge-reviewed 时间线节点；index 公共导出；新增 14 项测试，全量 344 绿（45 文件）；E1.3 升 ✅（多方案 noul 开放生成仍待后续） |
| v0.19 | 2026-09-24 | **E3.5 Realm write 与操作者授权凭证（库内部分）**：`FsRealmStore.write`（personal 默认允许、readOnly 拒、enterprise 须凭证；字符串/JSON 序列化、自动 itemId、tmp+rename 原子写、路径/symlink/扩展名/1MiB 防护、写后快照与 contentDigest 一致、审计回调），新增 `src/realm/grant.ts` `verifyDriverWriteGrant` 纯函数（missing/malformed/wrong-realm/expired 门），RealmStore 接口补 write、新增 UnauthorizedRealmWriteError/UnsupportedWriteError，index 公共导出；enterprise connect 与 MCP write tool 仍 P1；新增 22 项测试，全量 366 绿（46 文件）；E3.5 ⬜→🚧 |
| v0.20 | 2026-09-24 | **E8.3 Diary 日记 + E9.3 虚拟部门编制落地**：`src/diary` 把记忆事件按天叙事成可回溯（每行锚 eventId）、可经 Realm.write 落盘 / 稳定 JSON 导出的日记（19 测试，design-diary）；`src/org` 建立部门编制（单 lead、成员唯一、不可变）与 traceAccountability 责任链（执行 Agent → 部门 lead → 裁决操作者，无编制标 unassigned，15 测试，design-org）；全量 411 绿（49 文件），E8.3/E9.3 升 ✅ |
| v0.21 | 2026-09-24 | **Diary 与 Org 接通持久化与操作者 HTTP**：OrgRegistry 有状态编制纳入 KernelSnapshot 随 boot 重启恢复（8 测试）；Org HTTP（chart/建编/安置，6 测试）；buildDiariesFromState 按 realm 分组构建 + Diary HTTP（GET 读、POST generate 经 Realm.write 落盘，6 测试）；全量 431 绿（52 文件） |
| v0.22 | 2026-09-24 | **签名链 v1.1 internal 名册封签 + 容量四场景 + 评审 v0.4**：E5.2 internal `GET /api/roster` 由裸快照改为封签信封（attestation 扩 active\|revoked 两态，revoked 证永久吊销无硬过期、新鲜度由 seal maxAge 绑定，验签状态精确匹配防提升/掩盖、缺 source 或矛盾 fail-loud，no-store），public 路径不变，signing 新增 5 项；E10.4 容量基线升 v0.2，增 C（H2 门面全链路，附加 5–10ms/吞吐约低 10–15%）与 D（128 挂起分支 14–18ms 全取消、240 cancel 零丢失）；E4.9 仍 🚧（R1 internal 已补，待生产 RSK 托管/轮换与 R2 bayjf 公钥验签，deferred #7）；项目级评审刷新到 v0.4（库内已无 P0 功能缺口，产品级上线仅剩仓库外真机/凭证/发布动作）；全量 436 绿（52 文件） |
| v0.23 | 2026-09-24 | **操作者 HTTP 补面（接线批次，不新增内核能力）**：复核"库内已无可闭环任务"的判断后发现它只对 **P0 功能缺口** 成立——四块已落地能力经 HTTP 完全不可见，本批一次接上。① **Memory 面**（E8.5 补记行）：`/api/memory/{events,facts,recall,retractions,integrity}` + `POST retract\|forget-subject`，只读与擦除、无事实写入口；② **Skills 面**（E2.1/2.2/2.3/2.4）：目录检索·显式规格注册（逐字段白名单，任意 payload 写不进持久目录）·装卸/加固/废弃（越权加固 400、deprecated 重装 409）·组队；③ **Mentorship 面**（E2.5）：立项/授课/评估/作废，**不暴露 `grantProvider` 直调**（提供者身份只能经认证获得）；④ **Org 责任链**（E9.3）：`GET /api/org/accountability/:intentId`；⑤ **决策回放**（E1.6）：`GET /api/intents/:id/replay`（含 `?format=text`），配 `Orchestrator.getRequest()` 取回原始派发输入，无法回放的存储记录返回 500 而非半条时间线。新增 36 项测试，全量 **472 绿（56 文件）**；此前"未定位的并行 flaky"定位到成因（无 vitest 配置 × 5s 默认超时 × CPU 争抢），按先例放宽 vault L1 与 RSK keygen 两处，根治登记 deferred #11；README 纠正两处过期表述（internal 名册"不封签"、H3 SSE"按需立项"）并补 memory 模块行 |
| v0.24 | 2026-09-25 | **第二个"能一口气搞"批次（库内闭环 + 工程卫生，写作"7 个 commit"、2026-09-25 实测 9）**：① **E1.5 并发治理收口 ✅**——`src/orchestrator/semaphore.ts` 给 fan-out 加上进程内分支上界与 FIFO 有界队列（溢出即拒并把原因记进分支结果、租约 finally 归还、resumeBranch 同一道门；不配=沿用无界），`GET /api/metrics` 的 queueDepth 从此是真值，deferred #9 缩到"分流降级顺序"；② **E4.7 审计落盘与回读**——`ZEUS_AUDIT_FILE` → JSONL（并仍转发 stderr），`GET /api/audit` 按 runId/vassal/decision/limit 回读、按字节窗口读尾、损坏记录 500；**修掉 booted 内核里"吊销执行 Agent 不产生审计记录"**（bootKernel 从未接 `onRevoke` 治理桥，而 handoff 自 T1 起一直声称有 JSONL）；③ **E3.5 收口 ✅**——`connect` 不再拒 enterprise 且**如实存储 type**（此前 `realms.set` 硬编码 `personal`，令企业域写闸门在真实 store 上永不触发＝安全检查是装饰性的），UnsupportedRealmTypeError 随之删除；④ **E7 连接器面 + 决策面**——`/api/connectors*`（真跑握手、边界裁剪工具、**响应只报 hasToken 绝不清显 token**）、`GET /api/decision`；⑤ **安全修复**——内核状态文件写出固定 **0600**（内含连接器 bearer token 与记忆事实，此前默认 0644 本机任意账户可读）；⑥ 工程卫生——`vitest.config.ts` 全局 20s 取代三处逐套件补丁（并识破放宽救不了的那例：RSA keygen 换 EC，460ms）、CI actions 升 `@v7`、CI 首次真机双矩阵绿；⑦ 登记 deferred **#13**（内核状态文件进备份清单：实测阻塞在 restore L0 硬绑 RealmStore + 图字段语义）、**#14**（操作者凭证只有校验端、无签发与签名、nonce 不比对、write 无注入时钟）、**#15**（Node 20 EOL 却仍在矩阵内、无 engines 声明）。新增 44 项测试，全量 **512 绿（60 文件）** |
| v0.25 | 2026-09-25 | **纠偏批次（纯文档，零代码改动）**：我在按文档汇报进度时被用户纠正——**pr-helper 早已部署在生产且每天使用**（`https://pr-helper-ten.vercel.app`；核实：其 `vercel.json:9` 把 `/.well-known/agent-card.json` rewrite 到 `/api/a2a/agent-card`，`api/a2a/agent-card.ts` 是真实的 JSON-RPC + task store，`docs/verification-report.md:40` 记 Production 域），而 zeus 的 E4.8/M3/README/handoff 从 2026-09-21 起一直把"待 pr-helper 部署"当阻塞原因并被评审 v0.4 原样转述。**E4.8 状态仍 🚧，改的是归因**：真正缺的是"对线上执行一次验收"（zeus 与 pr-helper 两边都没有任何一次线上 A2A 验证记录），关闭命令已写进 E4.8 与 handoff Active work 40。M3 同步重估：**该里程碑缺的不是工程，是一次执行**。同时记一条纪律：本仓库的状态句（"待 X / 已 Y"）先核实再引用——同批另外抓到三例文档与代码不符（`dispatchAudit JSONL` 从未被进程使用、E4.7 治理桥从未接上、E3.5 企业域写闸门因 type 硬编码不可达），均已在 v0.24 修复 |
| v0.26 | 2026-09-25 | **收尾批次（今天前两批留下的债）**：① **审计文件写盘上限**——昨天那句 `appendFileSync` 无界增长会把盘写满、连带拖垮内核，而唯一缓解手段是"部署文档里写一句请自备 logrotate"；现默认 64MiB × 5 代自轮转（磁盘上界 = maxBytes×(keep+1)），配 `ZEUS_AUDIT_MAX_BYTES`/`ZEUS_AUDIT_KEEP` 与 `resolveAuditConfig` 严格校验，`0/off/unlimited` 为显式退出；测试主动植入缺陷证明闸门为真（40 条打 400B 上限：活动文件不越界、代数不超 keep、最旧确被丢弃、`maxBytes:Infinity` 不轮转）。② **E1.5 闸门贴上价签**——bench 场景 E + capacity-baseline v0.3；同进程 mock farm 下绝对值散布 ±2×，故只用不变量（in-flight==cap、peak queue==128−cap、零丢失）与比值（吞吐随 cap 近线性、尾延迟≈墙钟）下结论；顺带把 `branchTimeoutMs` 20×→40× 并标注为防卡死看门狗而非 SLO（满载时对连续尾段误触发过），新增廉价读数 `queueDepthNow()`（原先每 1ms 轮询 `snapshot()`，等于往被测窗口里加负载）。③ **`GET /api/escalations?kind=`**——缺参/意图冲突/记忆争议同队返回却需要完全不同的回答，`OversightDesk.list(status, kind)` 支持组合过滤。④ **`GET /api/state` 内核盘点**——`src/state/stats.ts` 的 `kernelStats(kernel)` 只报计数与路径（persistence / audit / 11 项计数），组装放库层而非 serve.ts 以便可测；**绝不服用快照内容**（内含连接器 token 与记忆明文），测试同时断言响应体与审计文件都不含该密钥。⑤ 新增 `Orchestrator.counts()` / `MemoryStore.counts()` 廉价读数（`exportState()` 深克隆全部，不能给轮询端点用）。新增 14 项测试，全量 **526 绿（61 文件）** |
| v0.27 | 2026-09-25 | **小面收尾 + 一个功能阻断**：① 编制**可减可换**——`POST /api/org/departments/:id/lead`（换 lead，原 lead 降为 member 不被丢弃）、`DELETE /api/org/departments/:id/members/:agentId`（此前只能加不能减）；不在编制内的 Agent 返回 **404 而非 400**，避免把"这人不在编制里"说成"你请求格式不对"；② `GET /api/escalations/:id` 支持按 id 轮询单条；③ `GET /api/memory/replay?realmId&runId` 把"事件 + provenance 命中事实"的离线回放搬到 HTTP；④ **中文名部门此前在任何入口都建不起来**——`departmentId = dept:<ascii slug>`，`slug('研发部')` 抛"yields no valid slug"，报错形态像输入有误、实为文字体系限制，而这个产品的主语言不是 ASCII；现无 ASCII 可提取时落到 `dept:<sha256(name) 前 8 位>`（确定性、URL 安全、同名同 id、异名异 id），显示名逐字保留，ASCII 名的 id 形态不变，`slug()` 严格契约不动。**发现路径值得记**：给 README 写撤岗示例时用了"研发部"这个名字和 `dept-yan-fa-bu` 这种写法——两处都错（建不起来 + 真实 id 带冒号），说明 org 段文档从未按真机跑过；已改为"用响应里的 departmentId 继续操作"并写明派生规则。真实进程冒烟全绿（含 `GET /api/state` 报审计 `64MiB x 5`、状态文件实测 `-rw-------`）。新增 6 项测试，全量 **532 绿（61 文件）** |
| v0.28 | 2026-09-25 | **E3.6 + E6.4 一批收口（企业域分级与双域授权）**：① `src/realm/tenant.ts` 三级 `TenantScope`（org/department/member，非 ASCII 段名可用、大小写折叠匹配、重连改租户即拒），**个人域不是租户**由 `connect` 抛错保证；层级是**结构性**边界（主体必须是 realm 租户的前缀或相等，可下探、不可上望旁视），**没有任何凭证能放宽它**。② `src/realm/authorization.ts` `DomainGrant` + `decideRealmAccess()` 单一判定（个人域→企业域须显式授权、企业域→个人域**永远拒**、reason 精确到 `grant-expired`/`access-not-granted`/`no-grant`）+ `DomainGrantRegistry`（**nonce 一次性**：吊销不等于复活，撤销后重放同 nonce 仍拒；随 KernelSnapshot 持久化，重启不静默放宽也不静默丢失）。③ **本轮真正的安全增量** `src/realm/source.ts`：此前 `realmHits` 全靠调用方自报、内核自己从没进过任何 Realm，"声明 personal 域却塞企业内容"无法发现；现在内核按 realmId 自取并**核对真实 type 与声明**，命中连同来源进派发，放行/拒绝都进同一根审计事件流（`domain-read`/`domain-refused`）。④ 运维面 `GET /api/domains`（**不含绝对路径**）、`POST/DELETE /api/domains/grants`、`GET /api/domains/access` 干跑探针，`/api/state` 增 `enterpriseRealms`/`tenantScopedRealms`/`domainGrants`；env `ZEUS_REALM_ENTERPRISE="<root>::<tenant>"`（缺/坏 tenant **拒启**）。⑤ **三个守卫都做过缺陷植入**：恒真 `tenantReaches` → 5 例红（库/真实目录/HTTP 三层）；关掉声明核对 → 2 例红；删掉企业→个人拒绝分支 → 3 例红；另发现并删掉一处**看起来在防护实则永不生效**的长度比较（植入后 0 例红）。⑥ 顺手修：`docs/design-http-transport.md` 的"HTTP 不挂任何 Realm 路由"约束精确化为**内容 vs 治理元数据**（design-realm §6.4），否则新代码与已裁决决策互相打脸。deferred **#6 销项**，新登记 **#17**（改边界需显式操作：当前只能重启且会被漂移检查拒启）、**#18**（MCP 侧 actor 判定，触发条件 E3.4 真执行 Agent）。新增 55 项测试，全量 **587 绿（66 文件）**，`tsc --noEmit`/build exit 0 |
| v0.29 | 2026-09-25 | **E9.1 + E9.2 一批收口（上岗门与首日简报）**：① 新组合层 `src/onboarding/`——**四道门现算不缓存**：seat（E9.3 名册）/ account（在册未吊销执行 Agent）/ authorization（`decideRealmAccess`：E3.6 租户 + E6.4 授权）/ mentorship（E2.5 certified，**出勤不算能力**）；记录只存"谁开档、谁签字、谁豁免了什么"，资格每次重算，所以**签字之后吊销执行 Agent 或撤出名册会自动失去资格**，不依赖有人记得改状态位。② 三条严格化：`requiredSkills` 为空必须**显式豁免并写理由**（"没人说这岗位要会什么"是发现不是放行）、豁免不得覆盖已声明要求、**座位租户不得比其 realm 更宽**（`acme/eng` 的部门里放 `acme` 级座位等于让它看见兄弟部门）。③ `composeBriefing` 只从已有事实装配：职责/责任链/已认证与还缺技能/谁能教/组织惯例（本部门 realm 记忆召回，跨域读抛错）/边界两向；**`culture.totalFacts` 与命中数分开报**（"什么都没写"≠"写了但无关"），**`canWrite` 只表示域边界**、企业写入另需 `DriverWriteGrant`（`writeNeedsGrant` 单列），内容去时间戳后取**稳定 digest** 供任务证明"照哪一版简报派的活"。④ `first-task` 真走一次 `fanOut`（realm 取连接现状真实类型、目标限定该 Agent、params 带 commissionId+digest），未签字 409 `commission_gate`+`blockedOn`；审计事件流新增 4 个 `commission-*` 决策；`/api/state` 增 `commissions`/`commissioned`；上岗记录进 KernelSnapshot。⑤ **真实进程冒烟揪出一个单测结构上看不见的洞**：fealty 缺 `dataRealms` 等字段的卡片能注册成功、派发时才以 `Cannot read properties of undefined (reading 'includes')` 崩——注册边界现按声明契约校验并点名缺哪个字段（空 `dataRealms` 合法＝"不承诺任何数据"，二极管会逐单给出理由）；三个测试 fixture 本身就是残缺卡（它们从没派过活所以一直没暴露），已补全。⑥ 门的可失效性三次植入验证（门常真 2 红 / 只信记录 3 红 / 能力门常过 2 红 / 校验关掉 4 红）。新增 33 项测试，全量 **620 绿（68 文件）**，`tsc --noEmit`/build exit 0；新文档 [design-onboarding](design-onboarding.md) v0.1 |
| v0.30 | 2026-09-25 | **deferred #13 + #14 一批收口（备份清单盖到内核状态文件 / 企业写凭证签名且一次性）**：① **Vault v2**——`TreasureMap.source`（`realm`（含 E3.6 tenant）\| `files` 白名单）、`inventoryFromFiles`（缺失/绝对路径/`..` 逃逸/非普通文件/软链/二进制/超 64MiB 七类硬拒）、`LiveSource` 端口取代 L0 里硬绑的 `RealmStore`、CLI `--files-root/--files/--tenant` 且 `check\|restore` 不再要挂载参数；**顺带修掉两个真实缺陷**：带租户 scope 的企业 Realm 校验时丢 scope 重连被吞成 "root unreachable"（恢复工具对健康语料库喊狼来了），以及白名单包含性用未解析 root 比较 realpath 文件（macOS `/var→/private/var` 下**每个**白名单文件都被判逃逸）。② **E3.5 凭证**——`issueDriverWriteGrant`（内核铸 nonce + Ed25519 签 JCS，复用 `registry/signing.ts`）、`verifyDriverWriteGrant` 改 async 并按 形状→绑定→**验签**→有效期判定、`DriverGrantLedger` 落盘前消费 nonce 且随内核快照持久化、`FsRealmStoreOptions.now` 注入时钟（消灭 `expiresAt: 2099-…` 那种伪装的"仍然有效"）；`POST /api/realm/write-grants`、日记缺授权返回 **403**、审计事件流 `driver-grant-issued`/`realm-write`、`GET /api/state` 报 `driverGrants.authority`（signed / shape-only 可见）。③ **验证**：`kernel.json` 毁库演练走真 CLI（备份→校验→删除→退出码 2→恢复→字节级 sha256 相同→用恢复出的文件 boot 起内核），签名/防重放/持久化三个守卫各做缺陷植入（去验签 2 红、账本永不拒 9 红、消费不落盘 1 红），编译产物真进程冒烟 13 项全过（含重启后仍拒重放）。全量 **658 绿（70 文件）**，typecheck/build 过 |
| v0.37 | 2026-09-26 | **离线验签入口 + 并行断言去墙钟化（Active work 54，全量 704 绿 / 73 文件，typecheck/build exit 0）**：① **E4.9/E5.4 的"可验证"从此有命令行入口**——`scripts/verify-roster.mjs`（`npm run verify:roster`）把 `verifySignedSnapshot` + `attestationMatchesCard` 接到操作者手上：`--url\|--file` 取件、`--key [keyId=]<公钥 PEM>`（可重复，供轮换期同时给新旧两把）、`--token`/`--header`、`--card name=path` 深比对、`--now` 让新鲜度可复现；退出码 **0 验过 / 1 被拒 / 2 用法与 I/O 故障**，把"名册是假的"和"我连不上"分成两类结论。两条约束：脚本 `import('../dist/registry/signing.js')` **用出货的那份实现**（另写一份 canonical JSON 就等于自己验自己的副本）；**深比对必须能让整次运行失败**——第一版打印 `MISMATCH` 却 `exit 0`，被自己的测试抓出来后改成计入拒绝。这条同时把 deployment §6 那条"封签经独立通道验签通过"从**长期不可执行**变成可执行。② **E1.1 的并行性断言不再测调度器**：`tests/orchestrator.test.ts` 用"两次派发开始时刻相差 <8ms"代理"两个分支同时在执行"，在 CPU 争抢下报 `expected 12 to be less than 8`（代码是对的，测的是机器）。改为在派发端口测试替身里统计并发峰值：并行例 `maxInFlight === 2`、串行对照 `=== 1`，两侧共用同一计数器，所以"这个谓词能分辨并行与串行"由对照对证明，不需要改编排代码。**全局 20s 超时天花板未动**——超时类（`vault` 24,998ms、`http-diary` 24,145ms 被杀；`acceptance-script` 单例子进程冷启动 ~10.2s）只在 `vitest.config.ts` 留下实测证据，放宽不是修复。③ **Active work 53 记的那条"身份未明的测试失败"闭合**（7 次连跑全绿 + 并发复现出上面两类），并顺带修掉本批自己在 A1 引入的**墙钟依赖**：fixture 以固定时刻封签、`maxAgeSeconds: 3600`，却不传 `--now`，于是 4 例在当天窗口过期后**必然全红而实现一行未改**；`run()` 现缺省注入 `--now`，全库扫过同一形状（`verifySignedSnapshot` 各调用点均已显式传 `now`；`org/department.ts` 的 `now = new Date()` 只写时间戳不判定）。④ **登记**：deferred **#23**（DAG 无驾驶员入口——它曾登记过，被**编号复用**静默覆盖，故本文件顶部补规则「编号只增不复用」）、**#24**（墙钟依赖目前无人守，可选做法是把系统时钟拨到未来跑一次全量）。基线 696 → **704**（+8 全在离线验签）。**本批未 push** |
| v0.38 | 2026-09-26 | **deferred #16 销项：CI 构建机钉 `ubuntu-24.04`（纯工作流改动，零代码/零测试改动）**：`ubuntu-latest` 自 **2026-10-19** 起指向 Ubuntu 26.04（actions/runner-images#14748）——换机器不需要改代码，也不需要任何人批准，而本项目刚拿到连续三次云端 CI 结论，这是当时唯一会在无人决定情况下变动的构建面。改为 `runs-on: ubuntu-24.04` 并在工作流内注明理由与代价（此后升镜像为**手动**动作，换了要复验）。**未动 Node 矩阵（20.x/22.x）**——"测哪些 Node"归 #15，"在谁的机器上测"归本条，两者不混。因此**未重跑全量基线**（改动不可能影响用例），评审 v0.13 与 README「已知风险」同步 |
| v0.39 | 2026-09-26 | **deferred #15 销项（选项 B，纯配置改动：代码与测试零改动）**：① **`engines.node: ">=22.0.0"`** + 新增 `.npmrc` 的 **`engine-strict=true`**——后者是本批的重点：实测不加它时 `npm ci` 在 Node 20 上只给 `npm warn EBADENGINE` 且 **exit 0**，声明形同虚设；加了之后**硬失败**。② 新增 **`.nvmrc`（22）**，让"该用哪个版本"机器可读（此前全仓库无 `.nvmrc`/`.npmrc`）。③ CI 矩阵 `[20.x, 22.x]` → **`[22.x, 24.x]`**，与 Docker `node:22-slim` 同线并把仍在发版的 24 纳入验证。④ 依据（上一批实测，非本批重跑）：20.20.2 / 22.23.1 / 24.20.0 三档按 CI 顺序**各 704 passed / 73 文件**、typecheck 与 build exit 0，且 **22/24 真进程冒烟**（`/healthz` 200、`GET /api/state` 200）；上游 `nodejs.org/dist/index.json` 显示 **v20 最后一次发布为 v20.20.2 / 2026-03-24**。⑤ **改后验证**：`npm ci --dry-run` 在 Node 20 上 EBADENGINE 硬失败、在 22 与 24 上无 engine 报错（传递依赖在 `engine-strict` 下也全过）；YAML 解析 `matrix=["22.x","24.x"]`。⑥ **代价已接受并写明**：本机 shell 仍是 20.20.2，`npm ci`/`npm install` 此后会失败，需 `fnm use 22`；已装依赖下 `npm run` 不受影响（Node 20 `npm run typecheck` exit 0）。**推送后云端已验证**（run 36254582377）：`ubuntu-24.04` 上 22.x / 24.x 双作业 success，无 ubuntu-latest 迁移注解 |
| v0.40 | 2026-09-26 | **deferred #23 销项（DAG 驾驶员入口，纯库内闭环）**：把 `src/orchestrator/dag.ts` + `dag-runner.ts` 已落地的图能力接到 H2 驾驶员面——`POST /api/intents` 接受可选 `dag:{nodes}`（依赖编码在节点 `dependsOn`，非另给 edges）、与 `body.skill` 互斥；结构校验（`parseDagSpec`）和图校验（`validateDag`，环/缺失依赖/重复 id）均 400 并点名；提交即回分层计划 + 关键路径 + 每节点状态；`GET /api/intents/:id/dag` 按 `dagId` 回读。关键设计：`DagRunner` 改为可接收**已存在的 Orchestrator**（boot 传入主 orchestrator），DAG 节点意图走同一个 orchestrator——共享幂等表、随快照持久化、`GET /api/intents/:id`（`${dagId}::${node}`）仍可读，不另造隔离执行器；部分失败跳过语义不变。验证：新增 `tests/http-dag.test.ts` 7 例，其中"两阶段意图 + 节点意图可追溯"用真进程 inject 冒烟（真实 HTTP + orchestrator + dispatcher 管线）；全量 **711 绿 / 74 文件**、typecheck/build exit 0。未做：CLI/MCP 面仍无 DAG 入口（触发条件②/③未到；MCP actor 判定归 #18）；DAG spec/result 在内存不随快照持久化（重启后 `GET :id/dag` 失忆，节点意图仍在）。**本批未 push** |
| v0.36 | 2026-09-26 | **修一处状态列与正文互相打脸（纯标记，零代码）**：E4.8（P0 标准客户端守护）的**状态列还写着 🚧**，而同一行的正文与 v0.33 演进行都记着「2026-09-25 首次对线上执行验收 PASS（关闭）」、handoff 亦记「E4.8 → ✅ 关闭」。改状态列为 ✅。**改后 P0 覆盖 26/26 全绿，全库不再有 P0 未闭合项**（其余未完成 8 项全在 P1–P3：E3.4 🚧、E4.9 🚧、E5.4、E10.2、E3.8、E4.10、E9.4、E8.4）。同时把同法扫出的其他「列 🚧 / 正文写已关闭」矛盾清零：**0 处**。诚实边界：这次线上验收的执行证据出自并行会话的记录（评审 v0.12 已标注"非本评审复跑"），标记为 ✅ 表示"该需求已执行并判定关闭"，不改变那条证据分级 |
| v0.35 | 2026-09-25 | **签名链 v1.2：版本闸（deferred #22 销项，全量 696 绿 / 72 文件，tsc/build exit 0）**：信封自 v1 就写 `v: 1`（`Attestation.v` / `Seal.v`，同一个 `ENVELOPE_VERSION`），但 `verifySignedSnapshot` **从不读它** —— 一个不被校验的版本字段是装饰性的，挡不住任何东西；而真正会随演进而变形的**载荷** `RosterSnapshot = { generatedAt, scope, entries }` 连这样一个字段都没有。现在：① 载荷加 `schemaVersion`（两个投影器写入常量 1），位置选在 `seal.snapshotDigest` **已覆盖的对象内**，因此不必改动签名输入就被签名保护，且两个位置只留一个真相；② 三道版本闸（`schemaVersion` / `seal.v` / `attestation.v`）**提到任何密码学之前**，拒绝原因点名是哪道闸——否则"格式太新"会表现为一个含糊的签名失败，而这两种处置完全相反；③ **向后兼容单向**：缺 `schemaVersion` 的旧件读作 1，摘要与签名自洽故仍可验签。**契约变更如实记录**：design-fealty-signing §4 设计要点原话"`RosterSnapshot` 结构不动"被这半句打破，已在原文标注并新增 §4.1。测试 4 例含**正向对照**（旧形状必须验得过，只在坏输入上测过的校验器可能永远错）与"把 `schemaVersion: 99` **重新签名**后仍被拒"（证明拒绝来自闸门而非摘要/签名顺带失败）。真进程实测 `GET /api/roster` 与 `/api/roster/public` 信封含 `"schemaVersion":1` 且 `seal.v` 不变。顺带修自己上一轮引入的 **138 处排版粘连**（`执行 Agent注册` → 加空格；余下 66 处 `Agent` 后接全角标点，本就不该加空格，未动） |
| v0.34 | 2026-09-25 | **执行 Agent 注册的错误分类修正（评审 v0.12 §B 抓到的缺陷，全量 692 绿 / 72 文件，tsc/build exit 0，Active work 49）**：`POST /api/vassals` 遇**连接层不可达**（对端连不上，而非返回坏状态）此前给 `400 invalid_request "fetch failed"` 且**丢失 URL**，与 E5.5/G1 承诺的"卡片不可达 502"相反——成因是 `registry.ts` 的 `fetchImpl` 外包无 try/catch，而 `card fetch failed: <status> <url>` 只在 `!response.ok` 时抛，undici 的裸消息既无前缀也无 URL，HTTP 层于是按消息正则把它落到 catch-all 的 400。现在新增 `CardFetchError`（连接层与非 2xx 同一类型、消息带 `cardUrl`），路由**按错误身份分类**取代消息正则：**transport 失败 → 502，卡片被拉到但内容不合格 → 仍 400**（"谁的错"的分界）。补两支从未被模拟过的失败用例（`fetchImpl` 抛异常）：**摘掉守卫恰好 2 例红**，而旧用例 `returns 502 when the card URL is unreachable` 仍然绿——它喂的是 500 状态而非连接失败，**用例名与实际覆盖不一致正是缺陷藏住的原因**。真进程复跑四条：连不上 → `502 … fetch failed (http://127.0.0.1:9/…)`、404 → `502 … 404 <url>`、有效卡片 → 201 且入名册、缺 `cardUrl` → 400 |
| v0.33 | 2026-09-25 | **E4.8 首次真机验收 PASS + docker 实构实跑 PASS（评审 v0.11）**：① **E4.8 关闭**——`scripts/acceptance-standard-a2a.mjs` 的 RPC 端点原写 `/api/a2a/tasks`（404），线上 pr-helper 的 JSON-RPC 面在 `/api/a2a/agent-card`（GET=卡片、POST=RPC，`vercel.json` rewrite 证实），修正后首次对线上执行验收：卡片发现（pr-helper、5 技能）+ `tasks/send` 接受（task 生命周期 input-required）、exit 0；**顺带纠偏"本沙箱到不了那个域"**——直连 TCP :443 超时，走本机系统代理（`127.0.0.1:7897`）+ Node 22 `NODE_USE_ENV_PROXY=1` 即通，阻塞原因再次从"环境不可达"更正为"没走代理"；② **docker 实构实跑**——`docker build -t zeus:0.1.0 .` + 镜像内 `gen-rsk-key.mjs` 生成密钥 + deployment.md §4.3 参数起容器（health check healthy）+ 带 token `GET /api/state` 返回完整内核状态（快照恢复可见），验证后清理。**E4.8 → ✅；M3 剩余**：Zeus↔loom 联调 + 起 Zeus 实例注册线上执行 Agent 真机扇出；**剩余仓库外动作**：Jev endpoint/key 核对、RSK 托管与公钥发布（deferred #7）、deferred #18 MCP actor 判定（等 E3.4 触发） |
| v0.32 | 2026-09-25 | **三纲执行点一批收口（全量 690 绿 / 72 文件，tsc/build 过，评审 v0.10 判 MVP = ✅ 库内达成）**：① **执行 Agent 出站凭证注入**——`VassalEntry.token?` + `registry.tokenFor(name)`（吊销/未知 undefined、吊销即刻断流）+ `withoutToken()` 剥 token（get/list/listAll 四视图绝不回显）+ seed 语法 `ZEUS_VASSAL_SEEDS="cardUrl\|token"`（`\|` 不在 RFC 3986 合法 URL 字符集、无歧义；空 token/空 URL 拒启）+ boot 接线 Dispatcher `tokenFor`，export/import 持久化（状态文件=派发凭证存储，0600 已保障）；测试 `tests/vassal-token.test.ts` 8 项。② **Skill 注册中心上派发路径**——`SkillGovernor.activeProviders` 三态（未注册→undefined pass-through / 注册无 active→`[]` 拒绝不派发），`fanOut` auto 路径过闸门（`refused-skill-uninstalled`/`refused-no-active-provider` 审计进脊、realm 用请求真实域），注册 spec `providedBy` 收窄 auto 目标，显式 `request.vassals` 一等旁路；测试 `tests/skill-governor.test.ts` 8 项。③ **MCP 服务端补 tools + 客户端调用面**——服务端 `tools/list`+`tools/call`（`realm.search`/`realm.read` 只读工具、**realmId 白名单即域边界**、不泄绝对路径、initialize 广告 tools）；客户端 `McpClient.callTool` + `ConnectorRegistry.callTool`（只调握手发现且经声明裁剪的工具）+ `POST /api/connectors/:id/tools/:name/call`（上游失败 502）；realm-mcp 12→15、mcp-connectors 9→11、http-connectors 9→12。**E2.3 卸载真夺权、E2.4 auto 目标过闸门、E7.1/E7.2 调用面真实存在、E4.8 出站认证已接线**。deferred #18（MCP actor 判定）与此批同落点，仍等 E3.4 真执行 Agent 触发 |
| v0.31 | 2026-09-25 | **评审 v0.9 之后的首跑正确性批次（E4.7 审计面 + 部署配置面；全量 665 绿 / 70 文件，tsc/build 过）**：① **审计失败不再冒充派发失败**——按 `.env.example` 默认值起服务时，每条意图分支的 `reason` 实测是 `ENOENT … audit.jsonl`（sink 假定目录存在，异常逃出 dispatcher 分支 `catch` 后被重新标成"执行 Agent 失败"；A/B 对照定位并复验修复）。现在构造期建目录与文件（审计路径不可用 → **拒启并点名 `ZEUS_AUDIT_FILE`**），运行期写失败只计数不抛（首次告警、后续静默计数），`GET /api/state` 增 `audit.failures/degraded`；写出固定 **0600**，且**轮转后重新收紧**（轮转正是把固定 mode 悄悄退回 0644 的那道缝）。② `ZEUS_REALM_ROOTS` 两扇门统一为逗号（MCP stdio 此前按 `[:;]` 切，冒号会切碎 Windows 盘符路径）——实测同一目录在 HTTP 内核与 stdio 进程里给出**同一个 realmId**。③ `.env` 由谁解析写进文件头（全仓库无 dotenv：docker `--env-file` 与 systemd 会读，裸机 `npm start` 不读，并给出 `node --env-file=` 这条真能用的路）。④ 启动日志逐条打印挂载了哪些 realm（id/类型/租户/可写性），`ZEUS_VASSAL_SEEDS` 拉不到卡片改为一行可读拒启。⑤ `deployment.md` §2 补上缺的 **10 行** env（代码读 24 个、表内原 14 行）。**6 次缺陷植入全部被接住**（4/1/1/1/1/1 例红；其中一次植入把源码改成语法错误，判为废植入、换合法等价植入重跑），真进程复跑三条。**E4.7 状态仍 ✅，改的是它能否在默认配置下工作；MVP 判定仍 ❌**（评审 §E 第 2–4 条未动：执行 Agent 出站凭证、Skill 上派发路径、MCP tools+actor）。另记一条**待决定**不一致：`ZEUS_JUDGE_THRESHOLD` 非数字被静默忽略（`tests/boot-decision.test.ts:70-76` 把它钉成契约），与 E1.5"非法值拒启"相反 |
