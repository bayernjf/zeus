# Without this, package.json "engines" is a warning only: npm ci on an
# unsupported Node exits 0 and prints EBADENGINE. Verified 2026-09-26:
# engines ">=22.0.0" + this flag makes npm ci fail on Node 20 and pass on 22.
engine-strict=true
